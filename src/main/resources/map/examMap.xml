<?xml version = "1.0" encoding = "UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD com.example.Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.graduate.mooc.mapper.ExamMap">

    <select id="queryIncompleteVideos" resultType="Integer">
        select count(1) from video where chid in (select chid from chapter where cid in(select cid from task
          where cid=#{param1} and exstate=0) and sno = #{param2} ) and play=0
    </select>

    <select id="queryCompletedSubjects" resultType="Integer">
        select count(1) from chscore where chid in (select chid from chapter where cid in(
            select cid from task where cid=#{param1})
            and exstate=0) and sno=#{param2}
    </select>

    <select id="queryCountChapters" resultType="Integer">
        select count(1) from chapter where chid in (select chid from chapter where cid in(
            select cid from task where cid=#{param1}) and exstate=0)
    </select>

</mapper>